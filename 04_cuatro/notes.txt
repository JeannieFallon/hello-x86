> cuatro files demonstrate memory access
  - cuatro_static.asm demonstrates the output of a static, unchanged string
  - cuatro_dynamic.asm demonstrates the output of a dynamic, updated string

> run commands (change file name for each example):
  nasm -f elf32 cuatro.asm -o cuatro.o
  ld -m elf_i386 cuatro.o -o cuatro
  ./cuatro
> expected outputs:
  cuatro_static     yellow
  cuatro_dynamic    Hello!
